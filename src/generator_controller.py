'''
Created on 8 jan. 2015

@author: Maurits
'''
#import generator
#import json
#class GeneratorController(object): #I guess this will have to be another singleton
#    '''
##    A wrapper for the generator class
 #   '''
#
#
 #   def __init__(self):
  #      '''
   #     this class keeps track of what types of dungeons should be generated at each level, it dousn't really store that much data,
    #    so you can just as well make a new instance every time
     #   '''
#    def generate_dungeon(self, dungeon_level):
 #       
  #  def register_special_room(self, ):
#TO DEFINE A DUNGEON ROOM:
self=object()
self.summon_minions=lambda x: None
self.say=lambda x: None
if False==True:
    if True is False:
        if self.name=="__main__":
            self.summon_minions("chewbaca")
        else:
            self.summon_minions("picacu")
    else:
        self.say("you don't have a minion to summon")
else:
    self.say("you don't have a minion")